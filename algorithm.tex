% SPACeR Training --- Algorithm 1, as implemented in spacer/train_spacer.py
% (post Test 22: rollout-accumulation K + single-forward PPO + paper road-cap)
% Compile + render to algorithm.png:
%   pdflatex algorithm.tex
%   pdftoppm -r 220 algorithm.pdf algorithm -png -singlefile
\documentclass[11pt]{article}
\usepackage[margin=1cm,paperwidth=22cm,paperheight=29cm]{geometry}
\usepackage{float}
\usepackage[linesnumbered,ruled,vlined]{algorithm2e}
\usepackage{amsmath,amssymb}
\pagestyle{empty}

\newcommand{\pth}{\pi_{\theta}}
\newcommand{\pref}{\pi_{\mathrm{ref}}}

\begin{document}
\begin{algorithm}[H]
\DontPrintSemicolon
\caption{SPACeR Training Test~22 --- as implemented (\texttt{spacer/train\_spacer.py}); $K\!=\!46$ paper-batch rollout-accumulation, single-forward PPO update, paper road-context cap}
\KwIn{frozen reference $\pref$ (CAT-K \texttt{clsft\_E9} SMARTDecoder, centralized, full-scene);\
      KL weight $\beta = 0.10$ (Variant~4 default; Test~21 swept $\{0.01,0.10,1.0\}$);\
      infraction weights $w_{\mathrm{coll}}=w_{\mathrm{off}}=0.75$;\
      $\alpha=0,\ w_{\mathrm{goal}}=0$ (Variant~4);\
      \textbf{rollout-accumulation factor $K=46$} (\texttt{-{}-accum-k 46}; gives
      samples/update $=126{,}576\!\approx\!131{,}072$, the paper Table~A3 \texttt{batch\_size});\
      iters $=80$ (Test~22 budget; killed at it67/80 after plateau, evaluated it50 ckpt);\
      paper Table~A3 PPO hyperparameters (\emph{unchanged from Test~18 onward}):
      $\gamma=0.99$, $\lambda=0.95$, clip $0.2$, $c_{\mathrm{vf}}=0.3$,
      $c_{\mathrm{ent}}=10^{-4}$, grad-clip $0.5$, $4$ epochs $\times$ $16$ minibatches,
      Adam lr $3\times10^{-4}$, seed $42$;\
      \texttt{roadgraph\_top\_k}$=120$ (paper~\S{A.3}: cap road-context per agent);\
      $W=24$ parallel worlds, $T=18$ token-steps per rollout,\
      injection: $5{,}000$-scene pool, $N=24$ fresh per micro-rollout.}
\KwOut{trained decentralized policy $\pth$}
\BlankLine
initialise $\pth$ --- late-fusion MLP: 2048-token actor $+$ critic \tcp*{\texttt{TokenPolicy}}
\For{each training iteration}{
  $\mathcal{B} \leftarrow \emptyset$ \tcp*{accumulation buffer (CPU)}
  \For{$k = 1, 2, \dots, K$ \emph{micro-rollouts on-policy w.r.t. frozen $\theta$}}{
    \If{$k > 1$ \textbf{or} \textnormal{outer-iter inject schedule fires}}{
      \texttt{inject\_scenes($N$ fresh from pool)} \tcp*{partial / full refresh}
    }
    \For{each of $W$ parallel worlds $(W \le 48)$}{
      reset simulator; observe state $s_0$ and scene context $c$\;
      \For{token-step $t = 0, 1, \dots, T$}{
        \ForEach{agent $i$}{$a_{i,t} \sim \pth(\cdot \mid o_{i,t})$ \tcp*{local ego-centric obs}}
        decode tokens $\rightarrow$ poses; step the simulator\;
      }
    }
    $\pref(\cdot \mid s) \leftarrow$ one forward over the rolled scene $\;=\; p(a_t \mid a_{<t},\, c)$ \tcp*{\texttt{score\_ref}}
    \ForEach{(agent, token-step)}{
      $r_{h} \leftarrow \log \pref(a_t \mid a_{<t},\, c)$ \tcp*{Eq.~3 \ (logged only; $\alpha=0$)}
      $r_{\mathrm{task}} \leftarrow -0.75\cdot\mathbf{1}[\mathrm{coll}] - 0.75\cdot\mathbf{1}[\mathrm{off}]$ \tcp*{Eq.~1, V4}
      $D_{\mathrm{KL}} \leftarrow \sum_{a} \pth(a\mid o)\,\log\!\big(\pth(a\mid o)\,/\,\pref(a\mid s)\big)$ \tcp*{Eq.~5}
      store $\langle o,\, a,\, r_{\mathrm{task}},\, \text{value},\, \log\pth\text{-logits},\, \pref\text{-logits}\rangle$\;
    }
    $A[r] \leftarrow \mathrm{GAE}(\gamma, \lambda)$ \tcp*{\texttt{\_gae}}
    append flat per-sample tensors to $\mathcal{B}$ on CPU \tcp*{\texttt{\_collect}}
  }
  \For{$4$ epochs $\times$ $16$ minibatches over $\mathcal{B}$ \emph{(now $K\!\cdot\!W\!\cdot\!T\!\cdot\!N$ samples)}}{
    move minibatch to GPU\;
    $(\textit{newlp},\ \mathcal{H},\ V,\ \log\pth) \leftarrow \texttt{policy.forward\_with\_logits}(o, a)$ \tcp*{single backbone fwd}
    $kl_{\mathrm{mb}} \leftarrow \sum_{a} \pth(a)\cdot\big(\log\pth - \log\pref\big)$ \tcp*{Eq.~5, recomputed}
    $\mathcal{L}_{\mathrm{PPO}} \leftarrow$ clipped-surrogate(\textit{newlp}, $A[r]$, clip $0.2$) $+\; c_{\mathrm{vf}}\cdot$value-loss $-\; c_{\mathrm{ent}}\cdot\mathcal{H}$\;
    $\mathcal{L}(\theta) \leftarrow \mathcal{L}_{\mathrm{PPO}} + \beta\cdot kl_{\mathrm{mb}}$ \tcp*{Eq.~2}
    $\theta \leftarrow$ Adam step on $\nabla\mathcal{L}(\theta)$;\ grad-norm clip $0.5$ \tcp*{\texttt{\_ppo\_update}}
  }
}
\Return $\pth$\;
\end{algorithm}

\vspace{6pt}
\noindent\footnotesize\textit{Test~22 result.} K=46 closes the per-update
batch-size gap to the paper (samples/update $=126{,}576\!\approx\!131{,}072$
Table~A3 value, 96\%). Trained for 50 outer iters
($\Rightarrow 50 \!\times\! K \!\times\! W \!\times\! T \!\times\! N \approx
6.3\!\times\!10^{6}$ env-steps; paper $10^{9}$, $\sim\!160\times$ short).
Phase-A eval on the new 941-scene validation set:
collision $=0.058$, off-road $=0.038$, \textbf{goal\_rate} $=0.014$,
\textbf{minADE} $=28.23\,$m vs the teacher's $0.293/4.21\,$m. \textbf{No
improvement over Test~21 b=0.10} --- per-update batch size and batch
quality are now empirically ruled out as causes of the persistent
degenerate ``safe-but-lost'' optimum. Remaining suspects: total
env-step budget (compute-bound) and vocab mismatch ($2{,}048$ tokens vs
paper's $\sim\!200$, locked by public \texttt{clsft\_E9}).
\end{document}
